language: generic
services:
  - docker
before_install:
  - docker build -t vitorpavanelli/react-test -f ./client/Dockerfile.dev ./client
script:
  - docker run -e CI=true vitorpavanelli/react-test npm test
after_success:
  - docker build -t vitorpavanelli/multi-client ./client
  - docker build -t vitorpavanelli/multi-nginx ./nginx
  - docker build -t vitorpavanelli/multi-server ./server
  - docker build -t vitorpavanelli/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push vitorpavanelli/multi-client
  - docker push vitorpavanelli/multi-nginx
  - docker push vitorpavanelli/multi-server
  - docker push vitorpavanelli/multi-worker
